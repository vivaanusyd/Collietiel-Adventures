# Allow everything. This is a public blog — there's nothing here to hide
# from a crawler, and drafts never reach the build in the first place
# (see src/lib/reviews.ts), so there's no draft URL to disallow.
User-agent: *
Allow: /

# Absolute URL is required here — the sitemap directive is the one place
# robots.txt doesn't accept a relative path. Keep it in step with `site:` in
# astro.config.mjs; those two are the only places the domain is written down.
Sitemap: https://collietiel-adventures.netlify.app/sitemap.xml
